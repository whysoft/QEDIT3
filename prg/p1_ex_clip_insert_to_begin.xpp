
	
	if( str_sub2cmd.find("clip insert to begin") == 0 )
	{
		//BD_MYLOG( "*-- page_butt ----**  " << str_sub2cmd << str_job );

		clip_insert_to_begin( this->m_my_pagepathname, str_text_main, str_text_sub );
	}

		

